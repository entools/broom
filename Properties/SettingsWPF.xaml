<Window x:Class="Revit.UserControl2"             
             x:Name="wins"
             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Revit"
             Icon="../settings.ico"
             mc:Ignorable="d" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Height="200" Width="400" Title="ENTOOLS BROOM">
    
    <Window.Resources>
        <ResourceDictionary Source="StyleButton.xaml"></ResourceDictionary>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBox ToolTip="Please, fill comma separated sheet and view name which need save" 
                 Text="Please fill this field" 
                 TextWrapping="Wrap" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" Name="tbSettingText"
                 Margin="5"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Style="{StaticResource GetButton}"  Width="100" Content="OK" Click="Button_OK"/>
            <Button Style="{StaticResource GetButton}" Margin="5" Width="100" Content="CANCEL" Click="Button_Cancel"/>
        </StackPanel>
    </Grid>

</Window>


